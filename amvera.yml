meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: app.py
  containerPort: 80
  persistenceMount: /data
